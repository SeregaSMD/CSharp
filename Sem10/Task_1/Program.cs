Console.Write("Введите число M: ");
int M = int.Parse(Console.ReadLine());
Console.Write("Введите число N: ");
int N = int.Parse(Console.ReadLine());

int NaturalSeries (int firstNumber, int secondNumber, int number)
{
    if (numbersecondNumber) return number;
    return NaturalSeries (firstNumber, secondNumber, number+1)
}